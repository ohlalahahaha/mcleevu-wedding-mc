name: Verify

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run build
      - name: Upload client preview build
        uses: actions/upload-artifact@v4
        with:
          name: mcleevu-client-build-${{ github.sha }}
          path: dist/
          if-no-files-found: error
          retention-days: 7
